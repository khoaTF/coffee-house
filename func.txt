CREATE OR REPLACE FUNCTION public.place_order_and_deduct_inventory(payload jsonb)
 RETURNS uuid
 LANGUAGE plpgsql
AS $function$ DECLARE new_order_id uuid; item jsonb; recipe_item jsonb; product_recipe jsonb; product_row record; deduction_qty numeric; current_stock numeric; req_ingredient_id uuid; item_quantity integer; total_reductions jsonb := '{}'::jsonb; p_tenant_id uuid; p_discount_code text; p_discount_amount numeric; BEGIN p_tenant_id := (payload->>'tenant_id')::uuid; p_discount_code := payload->>'discount_code'; p_discount_amount := (payload->>'discount_amount')::numeric; FOR item IN SELECT * FROM jsonb_array_elements(payload->'items') LOOP item_quantity := COALESCE((item->>'quantity')::integer, 1); SELECT recipe INTO product_recipe FROM public.products WHERE id = COALESCE(item->>'productId', item->>'id')::uuid AND tenant_id = p_tenant_id; IF product_recipe IS NOT NULL AND jsonb_array_length(product_recipe) > 0 THEN FOR recipe_item IN SELECT * FROM jsonb_array_elements(product_recipe) LOOP req_ingredient_id := (recipe_item->>'ingredientId')::uuid; deduction_qty := (recipe_item->>'quantity')::numeric * item_quantity; IF total_reductions ? req_ingredient_id::text THEN total_reductions := jsonb_set( total_reductions, ARRAY[req_ingredient_id::text], to_jsonb((total_reductions->>req_ingredient_id::text)::numeric + deduction_qty) ); ELSE total_reductions := total_reductions || jsonb_build_object(req_ingredient_id::text, deduction_qty); END IF; END LOOP; END IF; END LOOP; FOR req_ingredient_id, deduction_qty IN SELECT key::uuid, value::numeric FROM jsonb_each_text(total_reductions) LOOP SELECT stock INTO current_stock FROM public.ingredients WHERE id = req_ingredient_id AND tenant_id = p_tenant_id; IF current_stock IS NULL THEN RAISE EXCEPTION 'Ingredient % not found', req_ingredient_id; END IF; IF current_stock < deduction_qty THEN RAISE EXCEPTION 'Not enough stock for ingredient % (have: %, need: %)', req_ingredient_id, current_stock, deduction_qty; END IF; END LOOP; FOR req_ingredient_id, deduction_qty IN SELECT key::uuid, value::numeric FROM jsonb_each_text(total_reductions) LOOP SELECT stock INTO current_stock FROM public.ingredients WHERE id = req_ingredient_id AND tenant_id = p_tenant_id; UPDATE public.ingredients SET stock = current_stock - deduction_qty WHERE id = req_ingredient_id AND tenant_id = p_tenant_id; INSERT INTO public.inventory_logs (tenant_id, ingredient_id, change_type, amount, previous_stock, new_stock, reason) VALUES (p_tenant_id, req_ingredient_id, 'deduction', deduction_qty, current_stock, current_stock - deduction_qty, 'Xu?t kho t? D?t hï¿½ng (server-validated)'); END LOOP; IF p_discount_code IS NOT NULL THEN UPDATE public.vouchers SET used_count = used_count + 1 WHERE code = p_discount_code AND tenant_id = p_tenant_id; END IF; INSERT INTO public.orders ( tenant_id, table_number, session_id, customer_phone, earned_points, items, total_price, discount_code, discount_amount, order_note, payment_method, status ) VALUES ( p_tenant_id, payload->>'table_number', payload->>'session_id', payload->>'customer_phone', COALESCE((payload->>'earned_points')::integer, 0), payload->'items', (payload->>'total_price')::numeric, p_discount_code, p_discount_amount, payload->>'order_note', payload->>'payment_method', 'Pending' ) RETURNING id INTO new_order_id; RETURN new_order_id; END; $function$

